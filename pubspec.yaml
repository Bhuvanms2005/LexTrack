name: lextrack
description: "A legal case management application."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  sqflite: ^2.4.2
  path: ^1.9.1
  flutter_local_notifications: ^17.2.3
  workmanager: ^0.6.0        # <-- NEW: schedules background tasks
  http: ^1.2.1
  html: ^0.15.6
  url_launcher: ^6.3.2
  shared_preferences: ^2.5.4
  fl_chart: ^0.68.0
  pdf: ^3.10.8
  printing: ^5.12.0
  table_calendar: ^3.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true